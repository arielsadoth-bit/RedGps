services:
  redgps-exam:
    build: .
    container_name: redgps-exam
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      DATA_DIR: "/app/data"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
